<?php

$this->headTitle('Listar Resources');

?>

<div class="row">
    <?= $this->flashMessenger()->render('error', ['alert', 'alert-warning']); ?>
    <?= $this->flashMessenger()->render('success', ['alert', 'alert-success']); ?>
    <?= $this->flashMessenger()->render('info', ['alert', 'alert-info']); ?>
</div>

<div class="row">
    <a class="btn btn-success" href="<?= $this->url("admin/resource", ["action" => "create"]) ?>">Cadastrar</a>
</div>
<br/>

<div class="row justify-content-center">
    <div class="col-md-12">
        <table class="table">
            <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Nome</th>
                <th scope="col">Descrição</th>
                <th scope="col">Ações</th>
            </tr>
            </thead>
            <tbody>
            <?php foreach ($this->resources as $resource): ?>
                <tr>
                    <th scope="row"><?= $resource->getId() ?></th>
                    <td><?= $resource->getName() ?></td>
                    <td><?= $resource->getDescription() ?></td>
                    <td><a href="<?= $this->url('admin/resource', ['action' => 'show', 'id' => $resource->getid()]) ?>">Visualizar</a>
                        | <a href="<?= $this->url('admin/resource', ['action' => 'edit', 'id' => $resource->getid()]) ?>">Editar</a>
                        | <a href="<?= $this->url('admin/resource', ['action' => 'destroy', 'id' => $resource->getid()]) ?>">Excluir</a></td>
                </tr>
            <?php endforeach; ?>
            </tbody>
        </table>
    </div>
</div>